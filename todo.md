# TODO

- [ ] 设计工具调用扩展，允许统一调度代码执行、检索、数据库查询等外部工具，并将调用链写入 Trace。
- [ ] 构建多模型路由策略，按任务类型在不同模型间自动切换，支持草稿-润色等多阶段流程。
- [ ] 实现上下文缓存与复用机制，包括 prompt 缓存和部分回答重播，并在 Trace 中记录命中情况。
- [ ] 加入结构化输出校验（JSON/Schema），在核心层提供自动重试/修正逻辑以保证结果可消费性。
- [ ] 扩展响应分级合成流程，支持长文档分块总结与合成，并把拆分任务写入 Trace。
- [ ] 引入自适应采样与停止策略，根据 token/时间预算动态调整 temperature、max_tokens 或提前终止。
- [ ] 集成调用后分析钩子，支持自动评分、安全审查等插件式后处理。
- [ ] 实现弹性重试与降级机制，对常见错误自动重试或切换备份模型，并记录 backoff 信息。
- [ ] 增加并发批处理能力，控制并发度聚合批量请求结果，供 CLI/TUI/Web 使用。
- [ ] 提供自检/评估模式，让模型生成答案后自评并输出置信度。